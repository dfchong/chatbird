# COLLATE

1. 列的排序规则
2. 默认按数据类型（ 或 指定的默认排序规则）
3. 仅适用于字符串类型
4. 指定后的影响：
   1. ORDER BY
   2. 比较运算符：<  |  > | = | between
   3. 索引：按照指定的排序规则构建
5. 常见排序类型
   1. C / POSIX : 二进制字节码排序
   2. en\_US.utf8... : 基于特定语言文化习惯排序
   3. ucs\_basic : 基于unicode排序
   4. ICU：
      1. 不区分大小写
      2. 不区分重音
