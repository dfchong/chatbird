# 返回结果参数

| 参数名                 | 简要说明                     |
| ------------------- | ------------------------ |
| `output_alias`      | 为 `OLD` 或 `NEW` 行提供替代名称。 |
| `output_expression` | 插入或更新后要计算并返回的表达式。        |
| `output_name`       | 为返回的列指定名称。               |

