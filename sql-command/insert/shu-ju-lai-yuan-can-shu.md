# 数据来源参数

| 参数名              | 简要说明                    |
| ---------------- | ----------------------- |
| `DEFAULT VALUES` | 所有列都填入其默认值。             |
| `expression`     | 为对应列赋值的表达式或值。           |
| `DEFAULT`        | 对应列填入其默认值（标识列会生成新序列值）。  |
| `query`          | 一个 `SELECT` 语句，提供要插入的行。 |
